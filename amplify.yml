version: 1
frontend:
  phases:
    preBuild:
      commands:
        - env | grep -e NEXT_PUBLIC_ -e REMOTIE_ >> .env.production
        - npm ci
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: .next
    files:
      - "**/*"
  cache:
    paths:
      - node_modules/**/*
      - .next/cache/**/*
